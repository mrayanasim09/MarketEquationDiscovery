"""Source-specific, raw-to-canonical ingestion for the v2 study.

Ingestors verify archived acquisition payloads and map observed source fields into
the raw canonical schemas. They do not impute, interpolate, aggregate, or model.
"""
